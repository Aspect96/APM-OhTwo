/*import { actionTypes } from '../actions/index'

const auth = (state = {}, action) => {
  if (action.type = actionTypes.SAVE_USER) {
    state = {
      ...state,
      user: action.user
    }
  }
}

export default auth;

//Can be ignored for now. But I wanted to split it up, but it does not seem to work.

*/